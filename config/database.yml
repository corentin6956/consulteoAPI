# SQLite version 3.x
#   gem install pg
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'pg'
development:
  adapter: postgresql
  encoding: UTF8
  database: db/consulteo-dev
  username: postgres
  password: secret
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test:
  # adapter: postgresql
  # encoding: UTF8
  # database: db/consulteo-test
  # username: LocalUser
  # password: secret
  # pool: 5
  # timeout: 5000

# production:
  # adapter: postgresql
  # encoding: UTF8
  # database: db/consulteo-prod
  # username: LocalUser
  # password: secret
  # pool: 5
  # timeout: 5000
